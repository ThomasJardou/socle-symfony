<div class="flex flex-col font-sans">
    <button
            type="submit"
            class="transition duration-500 ease-in-out bg-green-500 py-3.5 hover:bg-green-600 rounded-md text-white uppercase {% if add_classes is defined %}{{ add_classes }}{% endif %}"
    >{{ text }}</button>
</div>
